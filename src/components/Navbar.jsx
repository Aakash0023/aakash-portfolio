import "../styles/navbar.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <h2>AAKASH</h2>

      <div className="nav-links">
        <a href="#">ABOUT</a>
        <a href="#">PROJECTS</a>
        <a href="#">CONTACT</a>
      </div>
    </nav>
  );
};

export default Navbar;
